{
  "name": "cubbyhole",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "./pocketbase serve",
    "watch": "esbuild --watch --bundle --minify --target=es6 src/index.tsx --outfile=pb_public/index.js",
    "build": "tsc --noEmit && esbuild --bundle --minify --target=es6 src/index.tsx --outfile=pb_public/index.js",
    "test": "mocha tests/*.test.js"
  },
  "keywords": [],
  "author": "border-radius",
  "license": "GPL-3.0-only",
  "devDependencies": {
    "esbuild": "^0.25.8",
    "mocha": "^11.7.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "pocketbase": "^0.26.2",
    "preact": "^10.26.9"
  }
}
